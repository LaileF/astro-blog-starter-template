---
---

<html lang="en">
<head>
  <title>SignalRoutes</title>
</head>

<body>

<div class="bg"></div>

<main id="scene">

  <!-- CENTER HUB -->
  <div class="hub">
    <div class="hub-glow"></div>

    <div class="hub-card">
      <img src="/logo.png" />
      <h1>SignalRoutes</h1>
      <p class="tag">Clear signals. Better decisions.</p>
      <p class="desc">
        A system of tools designed to cut through noise across money,
        business, news, and compliance—so you always know what matters
        and what to do next.
      </p>
    </div>
  </div>

  <!-- CARDS -->

  <div class="card tl">
    <img src="/founderroutes.png" />
    <h3>FounderRoutes</h3>
    <p>Build your business with clarity. Know what to do now—and what not to do.</p>
    <a href="https://founder-routes-app-rep-production.up.railway.app/" target="_blank">Visit →</a>
  </div>

  <div class="card tr">
    <img src="/spendscore.png" class="spend" />
    <h3>SpendScore</h3>
    <p>Understand your money in real time. Build better habits without complexity.</p>
    <span>Launching April 1</span>
  </div>

  <div class="card bl">
    <img src="/sourcebrief.png" />
    <h3>SourceBrief</h3>
    <p>The news, distilled. What matters. What’s true. Nothing extra.</p>
    <a href="https://forms.gle/ZmfuxiLXw5wnSC257">Join waitlist →</a>
  </div>

  <div class="card br">
    <div class="icon">⚖️</div>
    <h3>ComplianceIQ</h3>
    <p>Stay ahead of compliance without drowning in updates.</p>
    <span>Coming soon</span>
  </div>

</main>

<style>

/* BASE */
body {
  margin:0;
  font-family: system-ui, sans-serif;
  background:#f4f2ee;
  overflow:hidden;
}

/* BACKGROUND BASE */
.bg {
  position: fixed;
  inset: 0;
  background:
    radial-gradient(circle at center, rgba(180,140,40,0.20), transparent 60%),
    #f4f2ee;
  overflow: hidden;
}

/* CIRCUIT SYSTEM */
.bg::after {
  content: "";
  position: absolute;
  inset: 0;

  background-image: url("data:image/svg+xml;utf8,
  <svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 1200 800'>
    <g stroke='%2322c55e' stroke-width='1.2' fill='none' opacity='0.18'>

      <!-- RADIAL PATHS FROM CENTER -->
      <path d='M600 400 L300 150'/>
      <path d='M600 400 L900 150'/>
      <path d='M600 400 L300 650'/>
      <path d='M600 400 L900 650'/>

      <!-- BRANCHES -->
      <path d='M450 275 L450 220 L400 220'/>
      <path d='M750 275 L750 220 L800 220'/>
      <path d='M450 525 L450 580 L400 580'/>
      <path d='M750 525 L750 580 L800 580'/>

      <!-- EXTRA CIRCUIT DETAIL -->
      <path d='M600 400 L600 200'/>
      <path d='M600 400 L600 600'/>

    </g>
  </svg>");

  background-size: cover;
}

/* SIGNAL FLOW (SUBTLE MOTION) */
.bg::before {
  content: "";
  position: absolute;
  width: 600px;
  height: 600px;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);

  background: radial-gradient(circle, rgba(34,197,94,0.25), transparent 70%);
  animation: signalPulse 4s ease-in-out infinite;
  opacity: 0.25;
}

@keyframes signalPulse {
  0% { transform: translate(-50%, -50%) scale(0.9); opacity: 0.2; }
  50% { transform: translate(-50%, -50%) scale(1.2); opacity: 0.35; }
  100% { transform: translate(-50%, -50%) scale(0.9); opacity: 0.2; }
}

/* OPTIONAL NODE DOTS */
.bg::before {
  content:"";
  position:absolute;
  inset:0;

  background:
    radial-gradient(circle, rgba(34,197,94,0.4) 2px, transparent 3px);

  background-size: 180px 180px;
  opacity:0.15;
}

/* FLOATING SIGNAL */
.bg::before {
  content:"";
  position:absolute;
  inset:0;
  background:
    radial-gradient(circle, rgba(34,197,94,0.35) 0%, transparent 60%);
  animation: drift 14s linear infinite;
  opacity:0.15;
}

@keyframes drift {
  0% { transform: translate(-20%, -10%); }
  50% { transform: translate(20%, 10%); }
  100% { transform: translate(-20%, -10%); }
}

/* SCENE */
#scene {
  position:relative;
  height:100vh;
  transition: transform 0.08s ease-out;
}

/* HUB */
.hub {
  position:absolute;
  top:50%;
  left:50%;
  transform:translate(-50%, -50%);
}

.hub-glow {
  position:absolute;
  width:420px;
  height:420px;
  background:radial-gradient(circle, rgba(180,140,40,0.35), transparent);
  animation:pulse 3s infinite;
}

.hub-card {
  position:relative;
  width:380px;
  padding:32px;
  background:white;
  border-radius:20px;
  text-align:center;
  box-shadow:0 30px 80px rgba(0,0,0,0.12);
}

.hub-card img { height:70px; }

.tag { font-weight:600; }

.desc {
  font-size:14px;
  color:#555;
  line-height:1.5;
}

/* CARDS */
.card {
  position:absolute;
  width:360px;
  padding:28px;
  background:rgba(255,255,255,0.85);
  backdrop-filter: blur(6px);
  border-radius:20px;
  box-shadow:0 20px 60px rgba(0,0,0,0.08);
  transition: all .25s ease;
}

.card img { width:52px; }
.spend { width:42px; }

/* HOVER */
.card:hover {
  transform:translateY(-8px) scale(1.04);
  box-shadow:0 30px 100px rgba(180,140,40,0.35);
}

/* POSITIONS */
.tl { top:6%; left:6%; }
.tr { top:6%; right:6%; }
.bl { bottom:6%; left:6%; }
.br { bottom:6%; right:6%; }

/* ICON */
.icon {
  width:52px;
  height:52px;
  background:#fafafa;
  display:flex;
  align-items:center;
  justify-content:center;
  border-radius:10px;
}

/* ANIMATION */
@keyframes pulse {
  0% { opacity:0.4; transform:scale(1); }
  50% { opacity:1; transform:scale(1.15); }
  100% { opacity:0.4; transform:scale(1); }
}

</style>

<script>
const scene = document.getElementById("scene");

document.addEventListener("mousemove", (e) => {
  const x = (e.clientX / window.innerWidth - 0.5) * 10;
  const y = (e.clientY / window.innerHeight - 0.5) * 10;
  scene.style.transform = `translate(${x}px, ${y}px)`;
});
</script>

</body>
</html>